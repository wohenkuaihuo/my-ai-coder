# 保洁人员考勤与电子围栏数据抽检功能 — 需求分析报告

---

## 一、需求理解

**核心目标**：在现有考勤管理和轨迹监控体系之上，叠加一套**抽检机制**，由系统自动或人工触发，对保洁人员的考勤打卡数据和电子围栏轨迹数据进行抽样核查，形成抽检任务 → 执行核查 → 记录结果 → 异常上报的完整闭环，以防范供应商"吃空额"和数据造假。

---

## 二、现有系统能力盘点

在做抽检设计之前，先梳理现有系统已有哪些数据基础：

| 已有能力 | 数据来源 | 说明 |
|---|---|---|
| 打卡记录 | 海康打卡机实时同步 | 人脸识别，有时间戳 |
| GPS轨迹数据 | 小橙助手APP，5-10分钟上报一次 | 含坐标点、步数 |
| 电子围栏定义 | 系统预设广场地理边界 | 用于判断是否在岗在位 |
| 排班数据 | 排班表 | 知道谁今天应该上班 |
| 预警记录 | 系统自动生成 | 围栏外、轨迹疑似相同 |
| 考勤异常记录 | 系统自动检测 | 迟到/早退/缺卡 |

**结论**：原始数据已具备，缺少的是**抽检任务的生成、分配、执行、核销**这一层管理逻辑。

---

## 三、抽检对象拆解

抽检分为两个维度，需分别设计：

### 3.1 考勤数据抽检

**抽检什么？**
- 打卡记录的真实性（人脸是否本人、是否代打卡）
- 补卡记录的合理性（补卡原因、上传照片是否真实）
- 考勤异常处理是否合规（审批链是否走完）

**抽检方式：**
- **全员检查**：对某日/某时段所有在班人员的打卡记录进行核查
- **随机抽检**：系统按比例随机抽取人员（PRD已明确"一周全覆盖"）
- **定向抽检**：针对有异常预警的人员重点核查

### 3.2 电子围栏数据抽检

**抽检什么？**
- 员工工作时段内是否持续在围栏内
- GPS轨迹点是否连续（是否存在长时间无上报）
- 多人轨迹是否高度重合（疑似共用一部手机）
- 打卡位置与GPS位置是否一致（打卡机在围栏内，但GPS显示在围栏外）

**抽检方式：**
- **系统自动触发**：基于已有预警规则（围栏外、轨迹相同）自动生成抽检任务
- **人工发起**：管理员手动选择人员/时段发起抽检

---

## 四、核心业务流程设计

### 4.1 抽检任务生命周期

```
生成抽检计划
    ↓
系统自动抽样 / 人工指定抽样对象
    ↓
分配给检查人（物业专岗/主管）
    ↓
检查人执行核查（现场核实 or 数据比对）
    ↓
填写核查结果（正常 / 异常）
    ↓
异常 → 生成异常记录 → 触发处理流程
正常 → 任务关闭归档
    ↓
生成抽检报告（按月/按周汇总）
```

### 4.2 考勤抽检流程细化

```
触发方式：
  ① 定时任务（每周自动生成，确保一周全覆盖）
  ② 管理员手动发起

抽样规则：
  - 随机抽检：按专业类型分组，每组按比例抽取（建议20%-30%）
  - 重点抽检：本月补卡次数≥2次的人员必入抽检池
  - 全员检查：特定日期对全体人员检查

核查内容：
  ① 打卡时间与排班时间比对（系统自动完成）
  ② 打卡照片人脸与档案照片比对（人工 or 接腾讯云OCR）
  ③ 补卡记录审查（照片水印时间地点是否匹配）

核查结果：
  - 正常：记录归档
  - 异常：标记异常类型（代打卡/照片造假/无故缺岗等）→ 上报
```

### 4.3 电子围栏抽检流程细化

```
触发方式：
  ① 预警系统自动推送（围栏外预警 → 自动转为抽检任务）
  ② 定时抽检（每日随机抽取N人，核查其当日轨迹）
  ③ 人工发起（指定人员+时段）

核查内容：
  ① 轨迹完整性：工作时段内GPS上报点数是否达标（如每小时≥6个点）
  ② 围栏符合率：在围栏内的时间占比（建议阈值≥85%）
  ③ 轨迹相似度：与其他人员轨迹做相似度计算，超过阈值标记为疑似共用设备
  ④ 打卡-轨迹一致性：打卡时刻的GPS坐标是否在围栏内

核查结果：
  - 正常：归档
  - 异常：
    → 轨迹缺失：要求员工说明原因
    → 长期在围栏外：上报供应商，扣款依据
    → 轨迹高度重合：标记疑似作弊，人工复核
```

---

## 五、关键规则与防作弊设计

### 5.1 抽检防规避规则

| 风险点 | 防范措施 |
|---|---|
| 供应商提前知道抽检名单 | 抽检名单生成后加密存储，检查人执行时才解密显示 |
| 检查人与供应商串通 | 抽检任务随机分配检查人，且检查人不能核查自己管辖项目 |
| 补卡照片造假 | 强制使用马克水印相机，系统校验水印中的时间地点与补卡申请时间地点是否匹配 |
| GPS轨迹伪造 | 结合打卡机人脸数据做交叉验证；轨迹点速度异常（如瞬移）自动标记 |
| 多人共用一部手机上报轨迹 | 同一设备ID在同一时段出现多个员工的上报记录，自动预警 |

### 5.2 抽检覆盖率要求（来自PRD）

- 随机抽检：**一周内实现全员覆盖**
- 这意味着抽检系统需要记录每个员工最近一次被抽检的时间，优先抽取长时间未被抽检的人员（**加权随机算法**）

---

## 六、需要新增的数据结构

基于现有数据表，需新增以下表：

### 6.1 抽检计划表（inspection_plan）

| 字段 | 说明 |
|---|---|
| 计划ID | 主键 |
| 计划类型 | 考勤抽检 / 围栏抽检 / 综合抽检 |
| 抽检方式 | 全员 / 随机 / 定向 |
| 触发方式 | 系统自动 / 人工发起 |
| 计划时间范围 | 针对哪个时段的数据 |
| 抽检比例 | 随机抽检时的比例 |
| 状态 | 待执行/执行中/已完成 |
| 创建人/时间 | - |

### 6.2 抽检任务表（inspection_task）

| 字段 | 说明 |
|---|---|
| 任务ID | 主键 |
| 计划ID | 关联抽检计划 |
| 被抽检员工ID | 关联员工表 |
| 抽检数据类型 | 考勤记录ID / 轨迹记录ID |
| 分配检查人ID | - |
| 核查截止时间 | - |
| 核查状态 | 待核查/核查中/已完成 |
| 核查结果 | 正常/异常 |
| 异常类型 | 代打卡/轨迹造假/围栏外/轨迹缺失等 |
| 核查备注 | - |
| 核查时间 | - |

### 6.3 抽检异常记录表（inspection_anomaly）

| 字段 | 说明 |
|---|---|
| 异常ID | 主键 |
| 任务ID | 关联抽检任务 |
| 员工ID | - |
| 异常类型 | 枚举值 |
| 异常描述 | 详细说明 |
| 证据附件 | 截图/照片 |
| 处理状态 | 待处理/处理中/已处理 |
| 处理结果 | - |
| 是否上报供应商 | - |

---

## 七、与现有模块的关联关系

```
排班表
  ↓ 确定"今天谁应该上班"
  
考勤表 + 轨迹表 + 预警表
  ↓ 提供原始数据
  
抽检计划表
  ↓ 定义抽什么、怎么抽
  
抽检任务表
  ↓ 具体到每个人每条记录
  
抽检异常记录表
  ↓ 异常结果沉淀
  
考勤月报 / 看板
  ↓ 抽检通过率、异常率纳入统计
  
付款管理模块
  ↓ 抽检异常作为扣款依据
```

---

## 八、与三级看板的集成点

| 看板层级 | 新增展示内容 |
|---|---|
| 项目看板 | 本月抽检次数、抽检通过率、抽检异常人数 |
| 区域看板 | 各项目抽检覆盖率对比、异常率排名 |
| 总部看板 | 全国抽检数据汇总、高风险项目标红预警 |

---

## 九、关键风险与约束

1. **数据量风险**：近200个广场，每天GPS轨迹数据量极大，轨迹相似度计算需考虑性能，建议异步批处理而非实时计算。

2. **抽检公正性**：检查人分配需有隔离机制，避免利益冲突，建议系统强制执行"检查人不能核查本人管辖项目"规则。

3. **法律合规**：GPS轨迹数据涉及员工隐私，需在劳动合同或知情同意书中明确告知，数据保留周期需符合法规要求。

4. **与EKP审批的衔接**：抽检发现的重大异常（如代打卡）是否需要走EKP审批流程处理，需与业务方确认。

5. **补卡次数联动**：抽检发现某次补卡造假，该次补卡是否应从"3次免扣款机会"中扣除，需明确业务规则。

---

## 十、总结：需要新建的核心功能模块

| 功能模块 | 优先级 | 说明 |
|---|---|---|
| 抽检计划管理 | P0 | 创建/编辑/查看抽检计划 |
| 自动抽样引擎 | P0 | 加权随机算法，保证一周全覆盖 |
| 抽检任务分配与执行 | P0 | 检查人领取任务、填写核查结果 |
| 考勤数据自动比对 | P1 | 系统自动完成打卡时间vs排班时间的比对 |
| 轨迹合规性自动分析 | P1 | 围栏符合率、轨迹完整性、相似度计算 |
| 抽检异常处理流程 | P1 | 异常记录、上报、处理闭环 |
| 抽检报表与看板集成 | P2 | 抽检数据纳入三级看板和月报 |
| 预警转抽检自动联动 | P2 | 围栏外预警自动生成抽检任务 |